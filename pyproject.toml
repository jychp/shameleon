[tool.poetry]
name = "shameleon"
version = "0.1.0"
description = ""
authors = [
    "Jychp <jeremy.chapeau@resilience.care>"
]
readme = "README.md"
homepage = "https://github.com/jychp/shameleon"
packages = [{include = "shameleon_client"}]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
